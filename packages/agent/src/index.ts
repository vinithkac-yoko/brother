// Layer 3: Claude Agent SDK runtime. Single agent in Phase 1. dontAsk
// permission mode + PreToolUse hooks + deny rules. Thin system prompt;
// procedural knowledge lives in skills/.
//
// Populated in the "Agent runtime + skills" milestone (see claude-progress.md).

export const FAB_ERP_AGENT_VERSION = "0.1.0";
