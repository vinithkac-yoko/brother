# skills/_client/

Per-client skill overlay directory. Empty in the template. A client clone
may add or override skills here (e.g. client-specific quoting quirks)
without modifying the template skills directly, so template updates can
still be merged in cleanly.
